import styled from 'styled-components';

const StyledInput = styled.input`
    width: 100%;
    height: 40px;

    font-size: 32px;

    &:focus-visible {
        outline: none;
    }
`;

export { StyledInput };
